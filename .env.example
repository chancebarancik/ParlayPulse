# Neon or Postgres connection string for Vercel serverless functions.
POSTGRES_URL=postgres://user:password@host/db?sslmode=require

# Required for signing auth session cookies.
SESSION_SECRET=replace-with-a-long-random-secret

# Anthropic API key — used for AI analysis engine
ANTHROPIC_API_KEY=sk-ant-...
